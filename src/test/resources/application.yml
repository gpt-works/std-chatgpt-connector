webclient:
  gpt:
    authorization: "Authorization"
    bearer: "Bearer"
    apikey: "apikey-value"
    model: "gpt-4"
    max-tokens: 3000
    temperature: 0.7
    top-p: 1.0
    media-type: "application/json; charset=UTF-8"
    url: "mocked"
    organization: "OpenAI-Organization"
    organization-value: "oragnization-value"

logging:
  level:
    root: info

server:
  port: 18080